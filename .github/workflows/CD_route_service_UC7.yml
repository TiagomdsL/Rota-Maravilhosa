name: CD — route-service-uc7

on:
  push:
    branches:
      - main
    paths:
      - "route_service_UC7/**"
      - ".github/workflows/**"

  workflow_dispatch:

env:
  IMAGE: route-service-uc7
  PORT: 8003

jobs:
  build-and-smoke-test:
    name: Build + Smoke Test endpoints
    runs-on: ubuntu-latest

    steps:
      # ── 1. CHECKOUT ──────────────────────────────────────────────────────
      - name: Checkout código
        uses: actions/checkout@v4

      # ── 2. BUILD DA IMAGEM ───────────────────────────────────────────────
      - name: Build Docker image
        run: |
          docker build \
            -t ${{ env.IMAGE }}:${{ github.sha }} \
            -t ${{ env.IMAGE }}:latest \
            ./route_service_UC7

      # ── 3. ARRANCAR O CONTENTOR ──────────────────────────────────────────
      - name: Arrancar contentor
        run: |
          docker run -d \
            --name ${{ env.IMAGE }} \
            -p ${{ env.PORT }}:${{ env.PORT }} \
            -e API_TOKEN="" \
            -e PREDICTION_SERVICE_URL="http://localhost:8002" \
            -e OTEL_EXPORTER_OTLP_ENDPOINT="http://localhost:4318/v1/traces" \
            ${{ env.IMAGE }}:${{ github.sha }}

      # ── 4. AGUARDAR O SERVIÇO ESTAR PRONTO ──────────────────────────────
      - name: Aguardar serviço iniciar
        run: |
          for i in $(seq 1 20); do
            if curl -sf http://localhost:${{ env.PORT }}/health > /dev/null; then
              echo "Serviço disponível após ${i}s"
              exit 0
            fi
            sleep 1
          done

          echo "Timeout: serviço não respondeu"
          docker logs ${{ env.IMAGE }}
          exit 1

      # ── 5. HEALTHCHECKS ──────────────────────────────────────────────────

      - name: "GET /health → 200"
        run: |
          BODY=$(curl -sf http://localhost:${{ env.PORT }}/health)

          echo "$BODY" | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          assert d['status'] == 'ok'
          print('OK')
          "

      - name: "GET /ready → 200"
        run: |
          BODY=$(curl -sf http://localhost:${{ env.PORT }}/ready)

          echo "$BODY" | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          assert d['status'] == 'ok'
          print('OK')
          "

      - name: "GET /metrics → 200"
        run: |
          RESPONSE=$(curl -sf -D - http://localhost:${{ env.PORT }}/metrics)

          echo "$RESPONSE" | grep -i "text/plain"
          echo "$RESPONSE" | grep "requests_total"
          echo "$RESPONSE" | grep "request_latency_seconds"

      # ── 6. DEPENDÊNCIAS TESTES ───────────────────────────────────────────

      - name: Instalar dependências
        run: |
          pip install \
            fastapi \
            httpx \
            pytest \
            prometheus-client \
            opentelemetry-api \
            opentelemetry-sdk \
            opentelemetry-exporter-otlp-proto-http \
            opentelemetry-instrumentation-fastapi \
            opentelemetry-instrumentation-httpx \
            --quiet

      # ── 7. TESTE NORMAL ──────────────────────────────────────────────────

      - name: "POST /route/analyze — sucesso → 200"
        run: |
          python3 - <<'EOF'
          import sys
          from unittest.mock import patch, AsyncMock, MagicMock

          tracing = MagicMock()
          tracing.setup_tracing = MagicMock()
          tracing.get_current_span = MagicMock(return_value=MagicMock())

          sys.modules["tracing"] = tracing

          sys.path.insert(0, "route_service_UC7")

          from main import app
          from fastapi.testclient import TestClient

          #
          # Response síncrona
          #
          mock_response = MagicMock()

          mock_response.raise_for_status = MagicMock()

          mock_response.json = MagicMock(return_value={
              "accident_probability": 0.35,
              "predicted_severity": 2,
              "nearby_accidents_count": 3
          })

          #
          # POST async
          #
          async def mock_post(*args, **kwargs):
              return mock_response

          #
          # Client
          #
          mock_client = MagicMock()
          mock_client.post = mock_post

          #
          # async with httpx.AsyncClient()
          #
          mock_async_client = MagicMock()
          mock_async_client.__aenter__ = AsyncMock(return_value=mock_client)
          mock_async_client.__aexit__ = AsyncMock(return_value=None)

          with patch("httpx.AsyncClient", return_value=mock_async_client):

              client = TestClient(app)

              r = client.post(
                  "/route/analyze",
                  json={
                      "origin_lat": 34.05,
                      "origin_lon": -118.24,
                      "destination_lat": 34.10,
                      "destination_lon": -118.20,
                      "timestamp": "2022-01-15T08:00:00"
                  }
              )

              assert r.status_code == 200, (
                  f"Status: {r.status_code} — {r.text}"
              )

              data = r.json()

              assert "risk_score" in data
              assert "risk_level" in data
              assert "recommended_route" in data
              assert "total_waypoints_analyzed" in data

              print("OK — route analyze success")

          EOF

      # ── 8. TESTE VALIDAÇÃO ───────────────────────────────────────────────

      - name: "POST /route/analyze — campos em falta → 422"
        run: |
          python3 - <<'EOF'
          import sys
          from unittest.mock import MagicMock

          tracing = MagicMock()
          tracing.setup_tracing = MagicMock()
          tracing.get_current_span = MagicMock(return_value=MagicMock())

          sys.modules["tracing"] = tracing

          sys.path.insert(0, "route_service_UC7")

          from main import app
          from fastapi.testclient import TestClient

          r = TestClient(app).post(
              "/route/analyze",
              json={"origin_lat": 34.05}
          )

          assert r.status_code == 422

          print("OK — validation 422")

          EOF

      # ── 9. TESTE FALLBACK ────────────────────────────────────────────────

      - name: "POST /route/analyze — fallback → 200"
        run: |
          python3 - <<'EOF'
          import sys
          from unittest.mock import patch, AsyncMock, MagicMock

          tracing = MagicMock()
          tracing.setup_tracing = MagicMock()
          tracing.get_current_span = MagicMock(return_value=MagicMock())

          sys.modules["tracing"] = tracing

          sys.path.insert(0, "route_service_UC7")

          from main import app
          from fastapi.testclient import TestClient

          async def mock_post(*args, **kwargs):
              raise Exception("Prediction service unavailable")

          mock_client = MagicMock()
          mock_client.post = mock_post

          mock_async_client = MagicMock()
          mock_async_client.__aenter__ = AsyncMock(return_value=mock_client)
          mock_async_client.__aexit__ = AsyncMock(return_value=None)

          with patch("httpx.AsyncClient", return_value=mock_async_client):

              client = TestClient(app)

              r = client.post(
                  "/route/analyze",
                  json={
                      "origin_lat": 34.05,
                      "origin_lon": -118.24,
                      "destination_lat": 34.10,
                      "destination_lon": -118.20
                  }
              )

              assert r.status_code == 200

              data = r.json()

              assert data["risk_score"] == 0.0

              print("OK — fallback")

          EOF

      # ── 10. TESTE INTERPOLAÇÃO ───────────────────────────────────────────

      - name: "POST /route/analyze — interpolação"
        run: |
          python3 - <<'EOF'
          import sys
          from unittest.mock import patch, AsyncMock, MagicMock

          tracing = MagicMock()
          tracing.setup_tracing = MagicMock()
          tracing.get_current_span = MagicMock(return_value=MagicMock())

          sys.modules["tracing"] = tracing

          sys.path.insert(0, "route_service_UC7")

          from main import app
          from fastapi.testclient import TestClient

          call_count = 0

          async def mock_post(*args, **kwargs):
              global call_count

              call_count += 1

              mock_resp = MagicMock()

              mock_resp.raise_for_status = MagicMock()

              mock_resp.json = MagicMock(return_value={
                  "accident_probability": 0.5,
                  "predicted_severity": 2,
                  "nearby_accidents_count": 1
              })

              return mock_resp

          mock_client = MagicMock()
          mock_client.post = mock_post

          mock_async_client = MagicMock()
          mock_async_client.__aenter__ = AsyncMock(return_value=mock_client)
          mock_async_client.__aexit__ = AsyncMock(return_value=None)

          with patch("httpx.AsyncClient", return_value=mock_async_client):

              client = TestClient(app)

              r = client.post(
                  "/route/analyze",
                  json={
                      "origin_lat": 0.0,
                      "origin_lon": 0.0,
                      "destination_lat": 10.0,
                      "destination_lon": 10.0
                  }
              )

              assert r.status_code == 200

              assert call_count == 7, (
                  f"Esperado 7 chamadas, obtido {call_count}"
              )

              print("OK — interpolação")

          EOF

      # ── 11. LOGS ─────────────────────────────────────────────────────────

      - name: Logs do contentor
        if: failure()
        run: docker logs ${{ env.IMAGE }}

      # ── 12. CLEANUP ──────────────────────────────────────────────────────

      - name: Cleanup
        if: always()
        run: |
          docker stop ${{ env.IMAGE }} || true
          docker rm ${{ env.IMAGE }} || true